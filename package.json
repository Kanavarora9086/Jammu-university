{
  "name": "university-portal",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.0",
    "prettier": "^3.0.0"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "lint": "npm -w backend run lint && npm -w frontend run lint",
    "build": "npm -w backend run build && npm -w frontend run build",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,md}\""
  }
}
